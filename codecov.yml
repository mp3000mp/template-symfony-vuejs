coverage:
  round: nearest
  range: "50...90"
  status:
    project:
      backend:
        paths:
          - backend/src
        flags:
          - backend
      frontend:
        paths:
          - frontend/src
        flags:
          - frontend

flags:
  backend:
    paths:
      - backend/src
  frontend:
    paths:
      - frontend/src
